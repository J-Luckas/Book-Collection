/*
 * For a detailed explanation regarding each configuration property and type check, visit:
 * http
 */

export default {
  bail: true,

  clearMocks: true,
  coverageProvider: 'v8',
  preset: 'ts-jest',
  testMatch: [
    '**/*.spec.ts',
  ],
};
